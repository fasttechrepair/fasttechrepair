const menu=document.querySelector('.menu');const links=document.querySelector('.links');if(menu){menu.addEventListener('click',()=>links.classList.toggle('open'))}const reveals=document.querySelectorAll('.reveal');const io=new IntersectionObserver(es=>{es.forEach(e=>{if(e.isIntersecting)e.target.classList.add('show')})},{threshold:.12});reveals.forEach(el=>io.observe(el));document.querySelectorAll('[data-count]').forEach(el=>{let done=false;const obs=new IntersectionObserver(entries=>{if(entries[0].isIntersecting&&!done){done=true;let end=parseInt(el.dataset.count,10),n=0,step=Math.max(1,Math.ceil(end/60));let t=setInterval(()=>{n+=step;if(n>=end){n=end;clearInterval(t)}el.textContent=n+(el.dataset.suffix||'')},22)}},{threshold:.5});obs.observe(el)});

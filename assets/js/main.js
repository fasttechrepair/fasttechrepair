const menuBtn=document.querySelector('.menu-btn');const navlinks=document.querySelector('.navlinks');if(menuBtn){menuBtn.addEventListener('click',()=>navlinks.classList.toggle('open'))}document.querySelectorAll('.navlinks a').forEach(a=>a.addEventListener('click',()=>navlinks.classList.remove('open')));const io=new IntersectionObserver(entries=>{entries.forEach(e=>{if(e.isIntersecting)e.target.classList.add('visible')})},{threshold:.12});document.querySelectorAll('.reveal,.card,.metric').forEach(el=>io.observe(el));document.querySelectorAll('[data-count]').forEach(el=>{let done=false;io.observe(el);const obs=new IntersectionObserver(es=>{es.forEach(e=>{if(e.isIntersecting&&!done){done=true;let target=parseInt(el.dataset.count,10),n=0,step=Math.max(1,Math.ceil(target/60));let t=setInterval(()=>{n+=step;if(n>=target){n=target;clearInterval(t)}el.textContent=n+'+'},22)}})},{threshold:.8});obs.observe(el)});

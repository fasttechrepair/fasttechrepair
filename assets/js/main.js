
const menu=document.querySelector('.menu');const nav=document.querySelector('.navlinks');if(menu){menu.addEventListener('click',()=>nav.classList.toggle('open'))}document.querySelectorAll('.navlinks a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));const io=new IntersectionObserver(entries=>{entries.forEach(e=>{if(e.isIntersecting)e.target.classList.add('show')})},{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>io.observe(el));document.querySelectorAll('[data-count]').forEach(el=>{let done=false;const obs=new IntersectionObserver(es=>{if(es[0].isIntersecting&&!done){done=true;let target=parseInt(el.dataset.count,10),n=0,step=Math.max(1,Math.ceil(target/60));const t=setInterval(()=>{n+=step;if(n>=target){n=target;clearInterval(t)}el.textContent=n+'+'},22)}},{threshold:.4});obs.observe(el)});
